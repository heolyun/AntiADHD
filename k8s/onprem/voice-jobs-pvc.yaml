apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: antiadhd-voice-jobs
  labels:
    app.kubernetes.io/name: antiadhd-voice-jobs
    app.kubernetes.io/part-of: antiadhd
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
