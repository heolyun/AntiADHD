apiVersion: v1
kind: Service
metadata:
  name: antiadhd-backend
  labels:
    app.kubernetes.io/name: antiadhd-backend
    app.kubernetes.io/part-of: antiadhd
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: antiadhd-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
